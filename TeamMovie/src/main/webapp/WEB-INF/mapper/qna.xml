<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">

	<!-- 공지사항 리스트 불러오는 쿼리 -->
	<select id="selectQnaList" parameterType="qnaVO" resultType="qnaVO">
		select qnaNum, qnaCategory, qnaTitle, qnaComment, qnaRegdate
		from qna
		where
		qnaIsdelete = 'N'
        order by qnaNum 
        limit #{start},#{count}
	</select>
	
	<select id="selectCountQna" parameterType="qnaVO" resultType="int">
	select count(*)
	from qna
	where qnaIsdelete = 'N'
	</select>
	
	<!-- 공지사항 상세 페이지 불러오는 쿼리 -->
	<select id="getQnaContents" parameterType="int" resultType="qnaVO">
		select *
		from qna
		where qnaNum = #{value}
	</select>
	
	<!-- 공지사항 등록하는 쿼리 -->
	<insert id="qnaWrite" parameterType="qnaVO">
		insert into qna (qnaCategory, qnaTitle, qnaComment, qnaIsdelete, qnaRegdate)
		values ('QnA', #{qnaTitle}, #{qnaComment}, 'n', #{qnaRegdate})
	</insert>
	
</mapper>