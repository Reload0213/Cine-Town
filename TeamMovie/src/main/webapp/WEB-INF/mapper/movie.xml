<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="movie">
	  <select id="getMovieList" parameterType="movieVO" resultType="movieVO">
		  select mvNum, mvTitle, mvActor, mvGenre, mvContent1, mvYear, mvAge, mvRuntime, mvProducer, mvAvgRate, mvIsdelete, mvPosterPath
		 	 from movie
		  where mvIsdelete = 'N'
		  order by mvNum desc
	  </select>
	  
	  <select id="getMovieDetail" parameterType="movieVO" resultType="movieVO">
		  select mvNum, mvTitle, mvActor, mvContent1, mvContent2, mvContent3, mvContent4, mvGenre, mvYear, mvAge, mvRuntime,
		   mvProducer, mvAvgRate, mvIsdelete, mvPosterPath, mvBgPath, mvTrailer
		 	 from movie
		  where mvIsdelete = 'N'
		  and mvNum=#{mvNum}
	  </select>
  </mapper>